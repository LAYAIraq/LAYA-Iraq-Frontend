/**
 * @fileOverview tests if course navigation is complete
 * @author cmc
 * @since 1.3.0
 */

import '@/misc/course-navigation-complete'

describe('course navigation complete', () => {
  describe('courseNavDanglingIds', () => {
    it('returns true if course navigation is complete', () => {

    })
    it('returns false if course navigation is not complete', () => {

    })
  })
  describe('courteNavDanglingContent', () => {
    it('returns array of dangling content ids', () => {

    })
    it('returns empty array if no dangling content', () => {

    })
  })
})
